[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# SPA redirect - todas as rotas vão para index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
